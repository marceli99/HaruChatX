<%= turbo_stream.append "messages" do %>
  <article class="user-message">
    <%= @message.present? ? @message : t("conversations.empty.no_message") %>
  </article>

  <article class="assistant-message">
    <%= @reply.present? ? raw(@reply) : t("conversations.empty.no_reply") %>
  </article>
<% end %>
