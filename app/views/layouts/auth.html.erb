<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title><%= t('auth.page_title') %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "auth", "data-turbo-track": "reload" %>
</head>
<body class="auth-body">
<main class="auth-main" role="main">
  <section class="auth-card" role="region" aria-label="<%= t('auth.panel_aria') %>">
    <header class="auth-header">
      <h1 class="auth-title"><%= t('auth.heading') %></h1>
      <p class="auth-subtitle"><%= t('auth.subtitle') %></p>
    </header>

    <div class="flash-wrap" aria-live="polite" aria-atomic="true">
      <% if flash[:alert].present? %>
        <div class="flash flash--alert" role="alert"><%= flash[:alert] %></div>
      <% end %>
      <% if flash[:notice].present? %>
        <div class="flash flash--notice" role="status"><%= flash[:notice] %></div>
      <% end %>
    </div>

    <%= yield %>
  </section>
</main>
</body>
</html>
